{% extends "@PIFront/Front/index.html.twig" %}

{% block body %}
    <h1>Randonnee</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ randonnee.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ randonnee.text }}</td>
            </tr>

        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('Randonnee_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('Randonnee_edit', { 'id': randonnee.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
